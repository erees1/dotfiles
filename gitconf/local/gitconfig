[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
 [user]
    name = erees1
    email = edward.r.rees@zoho.com
[difftool]
    prompt = true
    # Be able to abort all diffs with `:cq` or `:cquit`
    trustExitCode = true
[diff]
    tool = nvimdiff
[difftool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""
[init]
    defaultBranch = main
[pull]
	ff = only
[pager]
    diff = delta
    log = delta
    reflog = delta  
    show = delta
[delta]
    line-numbers = true
    syntax-theme = 1337
    side-by-side = false
    file-modified-label = modified:
[interactive]
    diffFilter = delta --color-only
